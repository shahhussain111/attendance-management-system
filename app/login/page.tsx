"use client";
import { FormEvent, useState } from "react"; import { useRouter } from "next/navigation"; import { useAuth } from "@/components/auth-provider"; import { demoUsers, roleLabels } from "@/lib/auth";
export default function LoginPage() { const auth = useAuth(); const router = useRouter(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); function submit(event: FormEvent) { event.preventDefault(); const result = auth.login(email, password); if (!result.ok) return setError(result.message); router.replace("/"); } return <main className="min-h-dvh bg-slate-950 px-4 py-10"><div className="mx-auto grid max-w-5xl overflow-hidden rounded-2xl bg-white shadow-2xl lg:grid-cols-[1.05fr_.95fr]"><section className="bg-blue-600 p-8 text-white sm:p-12"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-white text-lg font-extrabold text-blue-600">N</span><p className="mt-10 text-sm font-semibold uppercase tracking-[.18em] text-blue-100">Northstar People Operations</p><h1 className="mt-3 text-4xl font-bold tracking-tight">Practice role-based attendance management.</h1><p className="mt-4 max-w-md text-blue-100">Explore tailored Admin, HR Manager, Manager, and Employee experiences without changing your HR data.</p><div className="mt-10 rounded-xl border border-white/20 bg-white/10 p-4 text-sm"><b>Practice security notice</b><p className="mt-1 text-blue-100">This is a local practice authentication system. It is not production security.</p></div></section><section className="p-8 sm:p-12"><h2 className="text-2xl font-bold">Sign in</h2><p className="mt-1 text-sm text-slate-500">Use any demo account below.</p><form className="mt-7 space-y-4" onSubmit={submit}><label className="form-field"><span>Email</span><input type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /></label><label className="form-field"><span>Password</span><input type="password" value={password} onChange={(e) => setPassword(e.target.value)} required /></label>{error && <div className="notice notice-error" role="alert">{error}</div>}<button className="btn-primary w-full" type="submit">Sign in</button></form><div className="mt-8"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Practice-only credentials</p><div className="mt-3 space-y-2">{demoUsers.map((user) => <button type="button" key={user.id} className="w-full rounded-lg border border-slate-200 p-3 text-left hover:border-blue-300 hover:bg-blue-50" onClick={() => { setEmail(user.email); setPassword(user.password); setError(""); }}><span className="text-sm font-bold">{roleLabels[user.role]}</span><span className="block text-xs text-slate-500">{user.email} · {user.password}</span></button>)}</div></div></section></div></main>; }
