"use client";
import { FormEvent, useState } from "react"; import { useAuth } from "@/components/auth-provider"; import { useOperations } from "@/components/operations-provider"; import { PageHeading } from "@/components/ui"; import { weekDays } from "@/lib/scheduling";
export default function SettingsPage() { const { role } = useAuth(); const { settings, updateSettings } = useOperations(); const [values, setValues] = useState(settings); const readOnly = role !== "admin"; function submit(event: FormEvent) { event.preventDefault(); updateSettings(values); } return <div className="space-y-6"><PageHeading title="Organization settings" description={readOnly ? "HR has read-only access to organization policy." : "Configure practice attendance and approval policy."} /><form className="panel" onSubmit={submit}><fieldset disabled={readOnly}><div className="grid gap-5 p-5 sm:grid-cols-2"><label className="form-field"><span>Organization name</span><input value={values.organizationName} onChange={(e) => setValues({ ...values, organizationName: e.target.value })} /></label><label className="form-field"><span>Timezone</span><input value={values.timezone} onChange={(e) => setValues({ ...values, timezone: e.target.value })} /></label><label className="form-field"><span>Default grace period</span><input type="number" min="0" value={values.defaultGraceMinutes} onChange={(e) => setValues({ ...values, defaultGraceMinutes: Number(e.target.value) })} /></label><label className="form-field"><span>Overtime policy</span><input value={values.overtimePolicy} onChange={(e) => setValues({ ...values, overtimePolicy: e.target.value })} /></label><label className="form-field"><span>Attendance percentage rule</span><input value={values.attendancePercentageRule} onChange={(e) => setValues({ ...values, attendancePercentageRule: e.target.value })} /></label><label className="form-field"><span>Leave approval mode</span><select value={values.leaveApprovalMode} onChange={(e) => setValues({ ...values, leaveApprovalMode: e.target.value as typeof values.leaveApprovalMode })}><option value="manager-and-hr">Manager and HR</option><option value="hr-only">HR only</option></select></label><fieldset className="sm:col-span-2"><legend className="text-sm font-semibold">Default working days</legend><div className="mt-2 flex flex-wrap gap-2">{weekDays.map((day) => <button type="button" className={`day-toggle ${values.workingDays.includes(day.value) ? "selected" : ""}`} key={day.value} onClick={() => setValues({ ...values, workingDays: values.workingDays.includes(day.value) ? values.workingDays.filter((item) => item !== day.value) : [...values.workingDays, day.value] })}>{day.short}</button>)}</div></fieldset><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={values.managerApprovalRequired} onChange={(e) => setValues({ ...values, managerApprovalRequired: e.target.checked })} /> Manager approval required</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={values.hrFinalApprovalRequired} onChange={(e) => setValues({ ...values, hrFinalApprovalRequired: e.target.checked })} /> HR final approval required</label></div></fieldset>{!readOnly && <div className="flex justify-end border-t border-slate-100 p-5"><button className="btn-primary">Save settings</button></div>}</form></div>; }
