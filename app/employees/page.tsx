"use client";
import Link from "next/link"; import { useMemo, useState } from "react"; import { useAttendance } from "@/components/attendance-provider"; import { useAuth } from "@/components/auth-provider"; import { Avatar, EmployeeStatusBadge, EmptyState, PageHeading } from "@/components/ui"; import { Icon } from "@/components/icons";
export default function EmployeesPage() { const { canManageEmployees, canDeleteEmployees, role } = useAuth(); const { employees, shifts, assignments, setEmployeeShift, setEmployeeActive, deleteEmployee } = useAttendance(); const [search, setSearch] = useState(""); const [department, setDepartment] = useState(""); const [status, setStatus] = useState(""); const departments = [...new Set(employees.map((employee) => employee.department))].sort(); const filtered = useMemo(() => { const query = search.trim().toLowerCase(); return employees.filter((employee) => (!query || [employee.name, employee.email, employee.id].some((value) => value.toLowerCase().includes(query))) && (!department || employee.department === department) && (!status || employee.status === status)); }, [employees, search, department, status]); const remove = (id: string, name: string) => { if (window.confirm(`Delete ${name}? Their historical records will be retained.`)) deleteEmployee(id); };
  return <div className="space-y-6"><PageHeading title={role === "manager" ? "My team" : "Employees"} description={role === "manager" ? "View your assigned team members only." : "Add, update, and manage everyone in your organization."} action={canManageEmployees ? <Link className="btn-primary" href="/employees/new">Add employee</Link> : undefined} /><div className="filter-bar"><label className="search-field"><Icon name="search" className="h-4 w-4" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search authorized employees" /></label><label className="filter-field"><span>Department</span><select value={department} onChange={(event) => setDepartment(event.target.value)}><option value="">All departments</option>{departments.map((item) => <option key={item}>{item}</option>)}</select></label><label className="filter-field"><span>Status</span><select value={status} onChange={(event) => setStatus(event.target.value)}><option value="">All statuses</option><option>Active</option><option>Inactive</option></select></label></div><div className="panel overflow-hidden"><div className="panel-heading"><div><h2 className="section-title">{role === "manager" ? "Assigned team" : "Employee directory"}</h2><p className="section-copy">{filtered.length} of {employees.length} visible employees</p></div></div>{filtered.length === 0 ? <EmptyState title="No employees found" description="Try adjusting your authorized search or filters." /> : <div className="table-wrap"><table><thead><tr><th>Employee</th><th>Department</th><th>Role</th><th>Shift</th><th>Status</th><th>{canManageEmployees ? "Actions" : "Profile"}</th></tr></thead><tbody>{filtered.map((employee) => <tr key={employee.id}><td><Link href={`/employees/${encodeURIComponent(employee.id)}`} className="flex items-center gap-3"><Avatar initials={employee.initials} /><div><p className="font-semibold text-slate-900">{employee.name}</p><p className="text-xs">{employee.id} · {employee.email}</p></div></Link></td><td>{employee.department}</td><td>{employee.role}</td><td>{canManageEmployees ? <select className="compact-select" value={assignments[employee.id] || ""} onChange={(event) => setEmployeeShift(employee.id, event.target.value)}><option value="">Unassigned</option>{shifts.map((shift) => <option key={shift.id} value={shift.id}>{shift.name}</option>)}</select> : shifts.find((shift) => shift.id === assignments[employee.id])?.name || "Unassigned"}</td><td><EmployeeStatusBadge status={employee.status} /></td><td><div className="flex gap-2">{canManageEmployees ? <><Link className="table-action" href={`/employees/${encodeURIComponent(employee.id)}/edit`}>Edit</Link><button className="table-action" onClick={() => setEmployeeActive(employee.id, employee.status !== "Active")}>{employee.status === "Active" ? "Deactivate" : "Activate"}</button>{canDeleteEmployees && <button className="table-action text-rose-600" onClick={() => remove(employee.id, employee.name)}>Delete</button>}</> : <Link className="table-action" href={`/employees/${encodeURIComponent(employee.id)}`}>View</Link>}</div></td></tr>)}</tbody></table></div>}</div></div>;
}
