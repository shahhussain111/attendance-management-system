"use client";
import { useParams } from "next/navigation"; import { EmployeeForm } from "@/components/employee-form"; import { useAttendance } from "@/components/attendance-provider"; import { EmptyState, PageHeading } from "@/components/ui";
export default function EditEmployeePage() { const { id } = useParams<{ id: string }>(); const { employees } = useAttendance(); const employee = employees.find((item) => item.id === decodeURIComponent(id)); if (!employee) return <EmptyState title="Employee not found" description="This employee may have been deleted." />; return <div className="space-y-6"><PageHeading title="Edit employee" description={`Update ${employee.name}'s profile and employment details.`} /><EmployeeForm employee={employee} /></div>; }
