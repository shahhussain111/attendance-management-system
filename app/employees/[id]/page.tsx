"use client";
import Link from "next/link"; import { useParams } from "next/navigation"; import { useAttendance } from "@/components/attendance-provider"; import { Avatar, EmptyState, EmployeeStatusBadge, PageHeading, ProgressBar, StatusBadge } from "@/components/ui"; import { defaultBalance } from "@/lib/leave"; import { formatShortDate, percentageFor } from "@/lib/attendance";
export default function EmployeeDetailsPage() {
  const { id } = useParams<{ id: string }>(); const { employees, records, leaveRequests, balances } = useAttendance(); const employee = employees.find((item) => item.id === decodeURIComponent(id)); if (!employee) return <EmptyState title="Employee not found" description="This profile is unavailable or has been deleted." />; const own = records.filter((record) => record.employeeId === employee.id).sort((a, b) => b.date.localeCompare(a.date)); const leaves = leaveRequests.filter((request) => request.employeeId === employee.id).sort((a, b) => b.requestDate.localeCompare(a.requestDate)); const count = (status: string) => own.filter((record) => record.status === status).length; const pct = percentageFor(records, employee.id); const balance = balances[employee.id] || defaultBalance();
  return <div className="space-y-6"><PageHeading title={employee.name} description={`${employee.role} · ${employee.department}`} action={<Link className="btn-primary" href={`/employees/${encodeURIComponent(employee.id)}/edit`}>Edit employee</Link>} /><section className="grid gap-6 xl:grid-cols-[1fr_1.5fr]"><div className="panel p-5"><div className="flex items-center gap-4"><Avatar initials={employee.initials} /><div><h2 className="text-lg font-bold">{employee.name}</h2><EmployeeStatusBadge status={employee.status} /></div></div><dl className="details-grid"><div><dt>Employee ID</dt><dd>{employee.id}</dd></div><div><dt>Email</dt><dd><a href={`mailto:${employee.email}`}>{employee.email}</a></dd></div><div><dt>Phone</dt><dd>{employee.phone}</dd></div><div><dt>Joining date</dt><dd>{formatShortDate(employee.joiningDate)}</dd></div><div><dt>Manager</dt><dd>{employee.manager}</dd></div><div><dt>Work location</dt><dd>{employee.location}</dd></div></dl></div><div className="panel p-5"><h2 className="section-title">Attendance summary</h2><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><div className="metric"><span>Present</span><b>{count("present")}</b></div><div className="metric"><span>Absent</span><b>{count("absent")}</b></div><div className="metric"><span>Late</span><b>{count("late")}</b></div><div className="metric"><span>Leave</span><b>{count("leave")}</b></div></div><div className="mt-5 flex items-center gap-4"><div className="flex-1"><ProgressBar value={pct} /></div><b>{pct}%</b></div><div className="mt-6 grid grid-cols-2 gap-3"><div className="balance-card"><span>Annual leave</span><b>{Math.max(0, balance.annualAllowance - balance.annualUsed)} remaining</b><small>{balance.annualUsed} of {balance.annualAllowance} used</small></div><div className="balance-card"><span>Sick leave</span><b>{Math.max(0, balance.sickAllowance - balance.sickUsed)} remaining</b><small>{balance.sickUsed} of {balance.sickAllowance} used</small></div></div></div></section>
    <section className="grid gap-6 xl:grid-cols-2"><div className="panel overflow-hidden"><div className="panel-heading"><h2 className="section-title">Recent attendance</h2></div>{own.length ? <div className="divide-y divide-slate-100">{own.slice(0, 6).map((record) => <div className="flex items-center justify-between px-5 py-3" key={record.date}><span className="text-sm font-medium">{formatShortDate(record.date)}</span><div className="flex items-center gap-2">{record.source === "approved-leave" && <span className="text-xs text-slate-400">Approved leave</span>}<StatusBadge status={record.status} /></div></div>)}</div> : <EmptyState title="No attendance history" description="Attendance entries will appear here once marked." />}</div><div className="panel overflow-hidden"><div className="panel-heading"><h2 className="section-title">Leave history</h2></div>{leaves.length ? <div className="table-wrap"><table className="min-w-[560px]"><thead><tr><th>Type</th><th>Dates</th><th>Days</th><th>Status</th></tr></thead><tbody>{leaves.map((leave) => <tr key={leave.id}><td>{leave.type}</td><td>{formatShortDate(leave.startDate)} – {formatShortDate(leave.endDate)}</td><td>{leave.days}</td><td><span className={`leave-badge leave-${leave.status.toLowerCase()}`}>{leave.status}</span></td></tr>)}</tbody></table></div> : <EmptyState title="No leave history" description="Leave requests for this employee will appear here." />}</div></section></div>;
}
